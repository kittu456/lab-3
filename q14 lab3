#include <iostream>
using namespace std;

int main() {
	int a1, a2, a3;
	cout << "Enter three angles: \n";
	cin >> a1 >> a2 >> a3;
	if(a1 + a2 + a3 == 180)
	{
		cout <<"The triangle is possible.";
	}
	else
	{
		cout << " The triangle is not possible. ";
	}
	return 0;
}
